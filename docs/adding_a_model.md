# 新增模型

1. 在注册表登记模型和 checkpoint，并完成 schema 校验。
2. 核验官方权重、代码、许可证、原生预处理和 embedding 接口。
3. 实现独立 Adapter，不修改评测指标以迎合模型。
4. 使用 10–20 张非敏感样例完成加载、shape、有限值和重复运行稳定性 smoke。
5. 使用统一 Frozen Feature Transfer 配置运行正式比较。
6. 更新支持矩阵：只有真实通过 smoke 的模型才能列入“已验证可运行”。

禁止仅因注册表存在名称就宣称模型已经支持。
