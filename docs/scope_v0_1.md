# v0.1 范围

v0.1 建立模型/checkpoint 注册表、Excel 导入器、校验器、生成目录、CLI、测试和 adapter 基础接口，不实现真实推理、权重下载或 benchmark。

第一阶段计划：CFP/OCT 视觉编码器 → 冻结特征提取 → 分类线性探测 → 统一评测。

第二阶段计划：零样本分类、图文检索、VQA、报告生成与分割。

`runtime_phase` 仅表示工程开发阶段，不表示模型质量排序。
