# Roadmap

1. 为 RETFound、VisionFM、MIRAGE、RETFound-Green、PRETI、DERETFound 实现并 smoke-test 图像编码 adapter。
2. 为 FMUE 增加不确定性专用评测接口。
3. 增加冻结特征提取、分类线性探测和统一数据协议。
4. 后续增加零样本分类、检索、VQA、报告生成和分割。
5. 经人工审核后再考虑定时模型发现、GPU benchmark workflow 与自动发布；v0.1 不创建这些 workflow。
